# stack-single 1.1.3

The notifier at 0.2.3 (heraldyx `docs/releases/v0.2.3.md`): the mail for a
provider that answered and refused says so, instead of describing it as
unreachable and free. With tokenfuse at 1.0.1 (1.1.2) that refusal reaches the
bus, so this is the other half of the same change. Nothing else in the pinned
set moves. **This pin moved without a run, by decision on 2026-09-14**; the
image is the same program with one sentence changed, held by its own test.

## The pinned set, 2026-09-14

As in 1.1.2, with `heraldyx:v0.2.3` in place of v0.2.2.

## Not in this release, said plainly

- No run; no mail from a 1.0.1 gateway's refusal has been read yet. The
  notifier needs `ALERT_TO` and an SMTP host to send anything at all.
