# stack-single 1.1.2

The money plane at 1.0.1, both images (tokenfuse `docs/releases/v1.0.1.md`:
an OpenAI cached token priced once, a one-hour cache write at its own rate, a
reachable provider's refusal on the bus). Nothing else in the pinned set
moves. **This pin moved without a run, by decision on 2026-09-14**: the three
corrections change what the gateway settles and what it writes to the bus,
not how it starts or answers; the tokenfuse tests, mutants and a second
model's review of the diff stand for them (estate-gates `PROVEN.md`), and the
1.1.1 run of the same morning stands for the rest of the set.

## The pinned set, 2026-09-14

As in 1.1.1, with `tokenfuse:v1.0.1` and `tokenfuse-control-plane:v1.0.1` in
place of v1.0.0.

## What to know on the way in

- An OpenAI-door run's `input_tokens` in the trace and the console is now the
  prompt count net of the cached subset (tokenfuse 1.0.1's note); a sum across
  the boundary mixes two definitions.
- A provider that refuses a call (a 429, a 5xx, a retired model id) now
  reaches the bus as `dependency_failed`; the notifier pinned here
  (heraldyx v0.2.2) describes it with the sentence for an unreachable
  provider until heraldyx's next release (heraldyx#66) is pinned.

## Not in this release, said plainly

- No run on this set; the next run of this launcher is where 1.0.1's settle
  figures are first seen on a box.
