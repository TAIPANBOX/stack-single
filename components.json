{
  "$comment": [
    "What this LAUNCHER installs, and what a check may believe about it.",
    "`checked` is proved against this repository by scripts/manifest-is-true.sh;",
    "`declared` is a statement nobody can verify and carries its own `why`.",
    "",
    "SAME QUESTION AS stack-up's MANIFEST, DIFFERENT SYNTAX, AND THAT IS WHY THE",
    "SECOND OPINION MATTERS. estate-gates' C5 reads all three launchers from one",
    "central file: `register <name>` out of a shell script here, compose service",
    "keys there, Kubernetes kinds in the third. Three grammars, one parser, none",
    "of it living where the fact lives. This is the other statement of the same",
    "fact, made where it does.",
    "",
    "IT SCHEDULES NOTHING, AND THAT IS THE LARGEST DIFFERENCE BETWEEN THE THREE.",
    "`schedules_routines` is empty and checked as empty, not omitted. stack-up",
    "runs six routines on a timer and stack-k8s runs them as CronJobs; this one",
    "installs no scheduler of any kind, so all six show up in estate-gates'",
    "deployment-parity file as recorded gaps. Declaring the emptiness here means",
    "the day somebody adds cron, this file has to say so rather than staying",
    "quietly true.",
    "",
    "THE IMAGES ARE PART OF WHAT IT INSTALLS. Unlike the other two launchers this",
    "one BUILDS what it runs, from source it fetches, because there is no public",
    "registry for these. Six are built by name and six more by a loop over",
    "`<service>:<repo>` pairs, and the check reads both forms: an image added to",
    "the loop is as invisible to a reader that only knows the explicit lines as",
    "an undeclared component is to a central registry.",
    "",
    "THREE PROFILES, AND TWO OF THEM ARE ALTERNATIVES. `egress` and",
    "`egress-browser` must not both be started: compose.yaml says so in its own",
    "comment. That is a constraint between profiles rather than a fact about any",
    "one of them, so it is declared rather than checked."
  ],
  "schema": "taipanbox.dev/components/v1",
  "repo": "TAIPANBOX/stack-single",
  "kind": "launcher",
  "components": [
    {
      "name": "stack-single",
      "class": "launcher",
      "checked": {
        "installs_services": [
          "caddy",
          "console",
          "focus-export",
          "heraldyx",
          "idryx",
          "idryx-detect",
          "init-volumes",
          "policy-db",
          "record-seal",
          "scopyx",
          "scopyx-browser",
          "tokenfuse-cloud",
          "tokenfuse-gateway",
          "verdryx-drift",
          "wardryx",
          "wg"
        ],
        "schedules_routines": {
          "focus-export": "focus-export",
          "verdryx-drift": "verdryx-drift",
          "record-seal": "trailryx-seal"
        },
        "manual_jobs": {
          "idryx-detect": "A TEMPLATE a person runs, not a schedule, and not by preference. Measured 2026-09-01 on the published image: `go-service.Dockerfile` builds on distroless, so there is no /bin/sh for a loop, the binary is `/usr/local/bin/service` rather than `idryx`, and `detect` has no --interval or --watch. A loop is impossible here rather than unwanted. Publishing a shell-bearing idryx would put a shell back into a governance plane to buy a scheduler, which is a security property traded for a convenience.\\n\\nSo it takes the shape the estate already has for exactly this: stack-k8s ships `costcrew-crew` as a suspended CronJob a person runs, and this is the compose equivalent, behind a profile no install enables. Run it with `docker compose run --rm idryx-detect`.\\n\\nVerified rather than asserted: manifest-is-true.sh requires the service to exist, to sit behind a profile, and requires install.sh never to pass that profile. That is this file's answer to the `suspend: true` stack-k8s's own gate checks."
        },
        "profiles": [
          "egress",
          "egress-browser",
          "manual",
          "record",
          "routines"
        ],
        "builds_images": [
          "stack/caddy:dev",
          "stack/genaryx-console:dev",
          "stack/heraldyx:dev",
          "stack/idryx:dev",
          "stack/scopyx-browser:dev",
          "stack/scopyx:dev",
          "stack/tokenfuse:dev",
          "stack/trailryx:dev",
          "stack/wardryx:dev",
          "stack/wg:dev"
        ],
        "pulls_images": [
          "ghcr.io/taipanbox/genaryx-console:v0.1.1",
          "ghcr.io/taipanbox/heraldyx:v0.2.2",
          "ghcr.io/taipanbox/idryx:v0.3.0",
          "ghcr.io/taipanbox/scopyx:v0.1.2",
          "ghcr.io/taipanbox/scopyx:v0.1.2-chromium",
          "ghcr.io/taipanbox/stack-caddy:v0.1.2",
          "ghcr.io/taipanbox/stack-wg:v0.1.2",
          "ghcr.io/taipanbox/tokenfuse-control-plane:v0.4.3",
          "ghcr.io/taipanbox/tokenfuse:v0.4.3",
          "ghcr.io/taipanbox/trailryx-node:v0.1.2",
          "ghcr.io/taipanbox/wardryx:v0.1.0"
        ]
      },
      "declared": {
        "egress_and_egress_browser_are_alternatives": {
          "value": "--profile egress OR --profile egress-browser",
          "why": "compose.yaml says it in its own comment: never both. That is a constraint between two profiles rather than a fact about either, and nothing here can check what an operator types."
        },
        "init_volumes_is_not_a_plane": {
          "value": "init-volumes",
          "why": "A one-shot busybox container that creates the shared volume layout and exits. It is in `installs_services` because compose starts it and this list is what compose starts; estate-gates records separately that it is not a component of the product."
        },
        "three_of_the_five_and_the_other_two_are_named": {
          "value": "--profile routines",
          "why": "This deployment installed NO scheduler of any kind until 2026-08-28: the planes came up and the work that keeps them producing did not happen. estate-gates recorded six divergences from 2026-08-06, on the single-box install, the one most likely to be somebody's first look at the estate.\\n\\nThree run now, as loops rather than timers, because compose has no cron. The other two are not included and the reasons are facts rather than effort:\\n  qryx-trend scans a SOURCE TREE, and there is none in these containers; mounting the operator's disk into a governance plane to look for crypto is a bigger decision than a missing routine.\\n  mockryx-drill needs scenario files that live in the mockryx repository and are not in this image, and it is opt-in in both other deployments anyway.\\n\\nThe sixth, trailryx-seal, is the `record` profile."
        },
        "trailryx_seal_runs_under_a_local_name": {
          "value": "record-seal",
          "why": "The service in the `record` profile IS trailryx-seal: the same `trailryx-node events` the other two launchers schedule, under this deployment's own name for it. stack-k8s does the same thing with five CronJobs whose names are all local. Listed under the ESTATE's name here, because that is the name the question is asked in."
        }
      }
    }
  ]
}
